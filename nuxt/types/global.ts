// TODO: need to add social-media links

type QuickLinkType = {
  label: string;
  href: string;
};

type ProjectLinkType = {
  website: string;
  name: string;
  icon?: string;
  description?: string;
};

export type { QuickLinkType, ProjectLinkType };
